name: Build plugin

# Builds VortarisModLoader on its native platforms, then packages ONE addon zip
# that contains the binaries for ALL platforms (Windows/Linux/macOS). On version
# tags the single zip is attached to the GitHub Release.
#
# Triggers:
#   - tag v*: build all platforms + package + attach to the Release
#   - manual: workflow_dispatch (build + package artifact, no release)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows x86_64
            os: windows-latest
            platform: windows
            arch: x86_64
            pkg: windows
          - name: Linux x86_64
            os: ubuntu-latest
            # godot-cpp v10 names this platform 'linux' (Godot's .gdextension
            # key / artifact name is 'linuxbsd'; SConstruct maps it).
            platform: linux
            arch: x86_64
            pkg: linuxbsd
          - name: macOS universal
            os: macos-latest
            platform: macos
            arch: universal
            pkg: macos

    env:
      GODOT_CPP_PATH: ${{ github.workspace }}/godot-cpp

    # Explicit bash shell everywhere: the Windows runner's default shell is
    # PowerShell, where `$VAR` does not expand environment variables.
    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4

      - name: Install SCons
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install scons

      - name: Checkout godot-cpp (v10 master, Godot 4.7 API)
        uses: actions/checkout@v4
        with:
          repository: godotengine/godot-cpp
          ref: master
          path: godot-cpp

      # api_version is REQUIRED by godot-cpp v10 master's EnumVariable.
      - name: Prebuild godot-cpp (debug)
        run: scons platform=${{ matrix.platform }} target=template_debug arch=${{ matrix.arch }} api_version=4.7 -j4
        working-directory: godot-cpp

      - name: Prebuild godot-cpp (release)
        run: scons platform=${{ matrix.platform }} target=template_release arch=${{ matrix.arch }} api_version=4.7 -j4
        working-directory: godot-cpp

      - name: Build plugin (debug)
        run: scons platform=${{ matrix.platform }} target=template_debug arch=${{ matrix.arch }} api_version=4.7 build_library=False godot_cpp_path="$GODOT_CPP_PATH" -j4

      - name: Build plugin (release)
        run: scons platform=${{ matrix.platform }} target=template_release arch=${{ matrix.arch }} api_version=4.7 build_library=False godot_cpp_path="$GODOT_CPP_PATH" -j4

      - name: Verify binaries exist
        run: ls -la demo/addons/vortarismodloader/bin/

      - name: Upload platform binaries
        uses: actions/upload-artifact@v4
        with:
          name: vml-bin-${{ matrix.pkg }}
          path: demo/addons/vortarismodloader/bin/
          if-no-files-found: error

  package:
    name: Package single all-platform addon
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all platform binaries
        uses: actions/download-artifact@v4
        with:
          path: dist/bins
          merge-multiple: true

      - name: Assemble one addon zip (all platforms)
        shell: bash
        run: |
          set -e
          rm -rf dist/_pkg && mkdir -p dist/_pkg/addons
          cp -r demo/addons/vortarismodloader dist/_pkg/addons/
          # Inject every platform's binaries (replaces bin/.gitkeep).
          cp -r dist/bins/. dist/_pkg/addons/vortarismodloader/bin/
          rm -f dist/_pkg/addons/vortarismodloader/bin/*.exp \
                dist/_pkg/addons/vortarismodloader/bin/*.lib \
                dist/_pkg/addons/vortarismodloader/bin/.gitkeep
          cp LICENSE dist/_pkg/addons/vortarismodloader/LICENSE
          python -c "
          import zipfile, os
          src = r'dist/_pkg/addons'
          dst = 'dist/vortarismodloader-${{ github.ref_name }}.zip'
          with zipfile.ZipFile(dst, 'w', zipfile.ZIP_DEFLATED) as z:
              for root, _, files in os.walk(src):
                  for f in files:
                      full = os.path.join(root, f)
                      z.write(full, os.path.join('addons', os.path.relpath(full, src)))
          print('packaged', dst)
          "
          ls -la dist/

      - name: Upload single addon
        uses: actions/upload-artifact@v4
        with:
          name: vml-addon-all
          path: dist/vortarismodloader-${{ github.ref_name }}.zip
          if-no-files-found: error

  release:
    name: Attach to GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: package
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: List artifacts
        run: ls -la dist/
      - name: Upload to Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release upload "$TAG" dist/*.zip --repo "$REPO" --clobber
          echo "Uploaded release assets for $TAG"
